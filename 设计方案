历史数据只存储一周 即 每个数据的生命周期为一周
redis 数据需要冷备份到mysql (极空间备份)
永远以股票代码为key
设立多个维度进行权重分析，需要界面来配置股票权重
需要添加维度 并进行向上兼容 比如


搜索筛选时需要按分数排名

其中a1 b1 为a b 因素的权重参数 a2 b2 为实际参数

总分数= a1*a2+b1*b2

总分数拿到后排序 

view界面展示最靠前的15支股票

考虑某只股票没有某个权重的因素参数（如esg） 权重应该为0上下 干扰只

数据输入和输出应当是分离的系统

输入仅负责数据导入 不做处理
 
输出系统进行处理

1:
redis util 专门用于写入数据的接口
初始化数据结构 func

写入某一字段的hash
初始化hash 的某个字段



